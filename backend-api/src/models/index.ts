// Make sure the User.ts file exists in the same directory, or update the path if it's located elsewhere.
import { User } from './User';
import { Post } from './Post';
import { Comment } from './Comment';

export { User, Post, Comment };